@if (config('klog.is_demo'))
    <div class="demo-banner" role="status">
        <span class="demo-banner__text">
            Demo instance · resets daily · not real data
        </span>
    </div>
@endif
